# Analizar el siguiente programa y explicar con tus palabras qué es lo que realiza.

"""
numeros = [8, 15, 3, 22, 7]
numeros.remove(max(numeros))
print(numeros)
"""

#El programa crea una lista llamada 'numeros', luego elimina el valor mas alto de la lista
#con la funcion 'max', por ultimo imprime la lista 'numeros' con el valor eliminado.